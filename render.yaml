services:
  - type: web
    name: cinema-flask-react
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd frontend && npm install && npm run build && cd ..
    startCommand: python backend/app.py
